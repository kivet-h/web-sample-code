/*
 * @Description: 菜单路由 icon
 */

import React from 'react';
import { HomeFilled, SettingFilled } from '@ant-design/icons';

export const IconMap: { [index: string]: React.ReactNode } = {
  home: <HomeFilled />,
  setting: <SettingFilled />,
};
